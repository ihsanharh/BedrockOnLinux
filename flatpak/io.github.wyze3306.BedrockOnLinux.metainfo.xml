<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.wyze3306.BedrockOnLinux</id>
  <launchable type="desktop-id">io.github.wyze3306.BedrockOnLinux.desktop</launchable>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>MIT</project_license>

  <name>BedrockOnLinux</name>
  <summary>Run Minecraft Bedrock on Linux</summary>

  <description>
    <p>
      BedrockOnLinux installs and runs the Windows (GDK) edition of Minecraft
      Bedrock on Linux. One app does everything automatically:
    </p>
    <ul>
      <li>downloads the Minecraft version you pick;</li>
      <li>fetches a GDK-Proton built from a WineGDK fork that implements XUser
          and request signing, so you sign in to Microsoft inside the game —
          no relay, no proxy;</li>
      <li>provides native Friends, invitations, public Servers and Realms
          without scanning or rewriting the Minecraft process;</li>
      <li>implements the WinAppSDK file picker used for world and skin imports;</li>
      <li>verifies the engine, fixes curl/SSL, then launches the game.</li>
    </ul>
    <p>
      Sign in, open Play &#8226; Servers, and join native Bedrock servers
      (Hive, CubeCraft, …) or crossplay/Geyser servers. No game files are
      shipped; you supply your own — you must own Minecraft.
    </p>
  </description>

  <categories>
    <category>Game</category>
    <category>ActionGame</category>
  </categories>

  <keywords>
    <keyword>minecraft</keyword>
    <keyword>bedrock</keyword>
    <keyword>gdk</keyword>
    <keyword>wine</keyword>
    <keyword>proton</keyword>
    <keyword>mcbe</keyword>
  </keywords>

  <url type="homepage">https://github.com/Wyze3306/BedrockOnLinux</url>
  <url type="bugtracker">https://github.com/Wyze3306/BedrockOnLinux/issues</url>
  <url type="vcs-browser">https://github.com/Wyze3306/BedrockOnLinux</url>

  <developer id="io.github.wyze3306">
    <name>Wyze3306</name>
  </developer>

  <branding>
    <color type="primary" scheme_preference="light">#84d160</color>
    <color type="primary" scheme_preference="dark">#23262e</color>
  </branding>

  <screenshots>
    <screenshot type="default">
      <caption>The launcher: pick a version, sign in, play</caption>
      <image>https://raw.githubusercontent.com/Wyze3306/BedrockOnLinux/v1.0.9/screenshot.png</image>
    </screenshot>
  </screenshots>

  <content_rating type="oars-1.1">
    <content_attribute id="social-chat">moderate</content_attribute>
    <content_attribute id="money-purchasing">mild</content_attribute>
  </content_rating>

  <releases>
    <release version="2.1.1" date="2026-07-25">
      <description>
        <p>Fixes custom-skin selection and restores Flatpak startup with safe
           data migration.</p>
        <ul>
          <li>completes the single-file picker on Minecraft&apos;s calling COM
              apartment instead of crossing into Wine&apos;s unsupported
              context callback;</li>
          <li>keeps the native picker attached to the game window in fullscreen
              and Gamescope sessions;</li>
          <li>keeps Flatpak&apos;s private data directory writable while
              migrating the previous data root read-only, fixing startup on
              Bazzite, CachyOS and other Flatpak hosts;</li>
          <li>ships the fix in the reproducible, attested
              <code>wow64-archs-native7</code> managed engine.</li>
        </ul>
      </description>
    </release>
    <release version="2.1.0" date="2026-07-25">
      <description>
        <p>Improves startup reliability across Linux desktops, LAN diagnostics
           and controller integration, and adds safer multi-profile and client
           workflows.</p>
        <ul>
          <li>handles X11, XWayland, Hyprland, Steam Game Mode, Steam Deck and
              multi-monitor dialogs automatically, including scaled and
              scrollable themed dialogs, global native-Wayland overrides and
              XWayland presentation recovery;</li>
          <li>fixes Minecraft&apos;s in-game world and custom-skin picker with a
              pinned native WineGDK implementation;</li>
          <li>migrates application data safely to the active XDG layout and
              keeps native, AppImage, Debian and Flatpak storage consistent;</li>
          <li>adds same-tag game-build refresh, LAN diagnostics and actionable
              guidance for discovery, version mismatches and full-world errors,
              without requiring environment variables;</li>
          <li>adds graphical GPU-crash acknowledgement, safer renderer
              diagnostics, automatic cleanup of completed launch markers and a
              guarded legacy compatibility renderer;</li>
          <li>configures SDL or Steam Input automatically for DualSense and
              DualSense Edge controllers;</li>
          <li>adds isolated local Xbox profiles with shared large downloads
              protected by setup and launch locks;</li>
          <li>repairs partial Wine prefixes and fixes Wine boot failures with a
              native cryptbase random-number implementation;</li>
          <li>hardens external client DLL injection and reports immediate game
              crashes instead of claiming a false success;</li>
          <li>strengthens downloads, archive imports, engine provenance,
              network diagnostics and release packaging.</li>
        </ul>
      </description>
    </release>
    <release version="2.0.0" date="2026-07-17">
      <description>
        <p>Adds native Xbox services, working Friends and Realms, stable world
           and skin imports, and removes the Minecraft process-memory patcher.</p>
        <ul>
          <li>implements native XGame, XUser, token signing, gamertags,
              privileges and XSAPI context in WineGDK;</li>
          <li>restores Friends, invitations, joining friends, public Servers
              and the dedicated Bedrock Realms authentication audience;</li>
          <li>implements single and multiple selection for the WinAppSDK
              FileOpenPicker contract in both Wine PE architectures so world
              and skin imports no longer crash;</li>
          <li>upgrades existing managed installations automatically and keeps
              engine replacement transactional and SHA-256 verified;</li>
          <li>records normal wrapper completion before Wine teardown so a
              delayed clean shutdown cannot become a permanent GPU block, while
              interrupted and legacy sessions remain fail-closed;</li>
          <li>ships reproducible WineGDK source provenance and rejects any
              remnant of the former Minecraft memory-patch implementation.</li>
        </ul>
      </description>
    </release>
    <release version="1.3.0" date="2026-07-13">
      <description>
        <p>Fixes the menu, ESC/back, pause, and dialog freeze reported in
           issues #27, #29, and #30 across both old and new Vulkan drivers.</p>
        <ul>
          <li>ships one reviewed vkd3d-proton 3.0.1 build containing both
              device-generated-command paths: EXT DGC on current drivers and
              the restored NV DGC fallback on older NVIDIA drivers;</li>
          <li>enables raw-VA root CBVs for Minecraft's indirect menu commands,
              preventing the render-thread stall while audio continues;</li>
          <li>removes the launcher's redundant Vulkan hardware probe and lets
              the universal engine choose EXT/NV inside the game process;</li>
          <li>blocks degraded FBDEV/software X11 sessions and recent fatal GPU
              kernel faults before Wine or Vulkan starts, preventing a broken
              host driver from turning PLAY into a full-system hard lock;</li>
          <li>records each GPU launch durably, checks the previous kernel boot,
              and refuses a blind retry after a power-loss interruption until
              the repaired incident is explicitly acknowledged;</li>
          <li>writes WineGDK/GameInput registry values offline and atomically,
              eliminating the pre-game UMU/Explorer processes; isolates the
              managed prefix and disables automatic kill/relaunch loops;</li>
          <li>fixes the 1.2.9 engine updater incorrectly treating the managed
              WineGDK engine as a custom engine, and makes engine replacement
              validated and transactional;</li>
          <li>reactivates the guarded Microsoft/Xbox server patches when an old
              compatibility preference survived an engine upgrade, while
              keeping an explicit per-revision troubleshooting fallback; and
              refuses incomplete or expired multiplayer token payloads, also
              on Python 3.9 and across Microsoft account changes;</li>
          <li>adds a precise diagnostic when an old engine cannot create
              Minecraft's ExecuteIndirect command signatures.</li>
        </ul>
      </description>
    </release>
    <release version="1.2.9" date="2026-07-11">
      <description>
        <p>Makes sure the graphics-engine fix from 1.2.8 actually reaches you:
           updating the launcher and pressing Play now updates the game engine
           automatically when a newer one has shipped. Before, the engine only
           updated if you re-ran Install/Update by hand, so the fix for the
           ESC/menu freeze silently missed people who just pressed Play — if it
           still froze for you on 1.2.8, this pulls the fixed engine in on the
           next launch.</p>
      </description>
    </release>
    <release version="1.2.8" date="2026-07-10">
      <description>
        <p>Fixes the game freezing when you press ESC / back, pause, or dismiss
           an in-game dialog — mostly on newer graphics cards and drivers.</p>
        <ul>
          <li>updates the bundled graphics engine (DXVK and vkd3d-proton) to
              3.0.1; the older version couldn't build one of Minecraft's draw
              commands and fell back to a path that hangs on recent GPUs, which
              is what caused the freeze on ESC / back / pausing / closing a
              popup;</li>
          <li>after updating, launch once to download the new engine.</li>
        </ul>
      </description>
    </release>
    <release version="1.2.7" date="2026-07-08">
      <description>
        <p>Fixes the game breaking when Advanced Diagnostics is on, and adds a
           clearer warning when your GPU isn't being used:</p>
        <ul>
          <li>with "Advanced diagnostics" enabled the game could hang or crash
              with broken menus — the verbose logging was flooding and starving
              the game; it now stays fast and playable with diagnostics on;</li>
          <li>if the game falls back to software rendering (your GPU's Vulkan
              driver isn't active), the launcher now tells you — that is what
              makes the Play screen render black; a reboot or driver
              reinstall fixes it.</li>
        </ul>
      </description>
    </release>
    <release version="1.2.6" date="2026-07-06">
      <description>
        <p>Fixes online sign-in and two crashes:</p>
        <ul>
          <li>the game no longer freezes on the "Get Started" screen with the
              music still playing — the online-login component it needs is now
              published, so signing in completes instead of hanging;</li>
          <li>fixes the crash when opening or closing Settings or pausing the
              game (seen mainly on the Steam Deck) by giving the game more
              stack room;</li>
          <li>adds an optional setting to keep the mouse cursor inside the
              window, so it no longer slips outside the game in windowed
              mode.</li>
        </ul>
      </description>
    </release>
    <release version="1.2.1" date="2026-06-18">
      <description>
        <p>Fixes a false "reinstall the game engine" warning that could appear
           after closing the game even when everything had worked: the
           post-game check was matching an unrelated, harmless system message.
           It now trusts the engine's own sign-in markers, so it no longer
           cries wolf on a perfectly good session.</p>
      </description>
    </release>
    <release version="1.2.0" date="2026-06-18">
      <description>
        <p>A refreshed, modern interface and a fix for launch crashes on some
           systems:</p>
        <ul>
          <li>redesigned launcher with rounded, flat controls and crisp
              antialiased text — and it is fully self-contained, so it looks
              the same on every distribution with nothing to install;</li>
          <li>a new setting to turn off the in-game Servers-tab unlock: on a
              few systems forcing it makes the game freeze or crash a few
              seconds after launch, and turning it off lets the game run
              (Steam Deck, CachyOS and some others);</li>
          <li>the version list and downloads are more reliable, and downloads
              now resume instead of failing on a flaky connection.</li>
        </ul>
      </description>
    </release>
    <release version="1.1.6" date="2026-06-18">
      <description>
        <p>Fixes the AppImage hanging at "Preparing" with an empty version list
           on some Linux distributions. It now bundles its own certificate
           store, so it can always reach the download servers over HTTPS no
           matter where the system keeps its certificates — the version list,
           the game engine download and Microsoft sign-in all work out of the
           box. Completes the self-contained AppImage from 1.1.5.</p>
      </description>
    </release>
    <release version="1.1.5" date="2026-06-18">
      <description>
        <p>The AppImage is now fully self-contained: it bundles its own Python
           and the Tk GUI toolkit, so it runs on any Linux out of the box with
           nothing to install first. This fixes the "tkinter is not installed"
           error on systems that ship Python without the Tk module. The Flatpak
           was already self-contained and the Debian package pulls those
           pieces in automatically; the portable .pyz still uses the system
           Python.</p>
      </description>
    </release>
    <release version="1.1.4" date="2026-06-18">
      <description>
        <p>Fixes the launch crash on recent Minecraft builds (1.26.30+) the
           proper way, so the in-game Servers tab is unlocked everywhere
           instead of being greyed out on those builds. The "is signed in with
           Microsoft" patch now guards against the account object the game has
           not finished setting up yet: it keeps the flag where the object
           exists and safely does nothing where it is missing, so the game
           starts on every system (Steam Deck, CachyOS and others) and still
           shows the server list. Delivered by the engine auto-update.</p>
      </description>
    </release>
    <release version="1.1.3" date="2026-06-18">
      <description>
        <p>Fixes a crash a few seconds after launch on recent Minecraft builds
           (1.26.30+): a binary patch was forcing an "is signed in with
           Microsoft" flag that those builds trust to access an account object
           that does not exist under Wine, causing a null dereference. The
           patch is now only applied on builds where it is safe; newer builds
           run normally (with a greyed Servers tab) instead of crashing. The
           fixed engine is delivered by the engine auto-update.</p>
      </description>
    </release>
    <release version="1.1.2" date="2026-06-17">
      <description>
        <p>The bundled game engine now auto-updates: when a newer WineGDK
           engine is published, the launcher re-downloads it on the next
           launch — the same way the launcher updates itself — so the in-game
           sign-in and server-join fixes reach everyone, not just fresh
           installs.</p>
      </description>
    </release>
    <release version="1.1.1" date="2026-06-17">
      <description>
        <p>Fixes after 1.1.0:</p>
        <ul>
          <li>fixes a crash on the first launch after install, caused by
              Proton's OpenXR runtime — VR is now fully disabled so the game
              starts straight into normal (flat) mode;</li>
          <li>the version picker now defaults to the version you last played
              (or the latest) instead of resetting to a different one;</li>
          <li>the in-game sign-in and native-server-join binary patches now
              track the game across the 1.26.x updates.</li>
        </ul>
      </description>
    </release>
    <release version="1.1.0" date="2026-06-17">
      <description>
        <p>Internal refactor into a Python package, with fixes for the open
           bug reports:</p>
        <ul>
          <li>fixes a startup crash and a spurious Steam launch caused by
              Proton's VR client;</li>
          <li>initialises the Wine prefix before installing the input and
              login components, fixing a "system32 not found" failure on a
              fresh install;</li>
          <li>fixes the Flatpak failing to start under a pure Wayland
              session;</li>
          <li>makes Microsoft sign-in reliable by ensuring the required
              Python dependency is present on every install.</li>
        </ul>
      </description>
    </release>
    <release version="1.0.9" date="2026-06-11">
      <description>
        <p>Flatpak packaging, plus two Wayland input fixes:</p>
        <ul>
          <li>opt-in gamescope (BOL_GAMESCOPE=1) so the in-game mouse works on
              Wayland sessions, where XWayland otherwise drops it;</li>
          <li>the opt-in winewayland backend now falls back to XWayland
              automatically when it cannot open a window, so the game always
              starts.</li>
        </ul>
      </description>
    </release>
    <release version="1.0.8" date="2026-06-11">
      <description>
        <p>Fixes for the opt-in winewayland backend and safer prefix repair.</p>
      </description>
    </release>
  </releases>
</component>
